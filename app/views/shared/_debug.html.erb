<% if Rails.env.development? %>
    <div class="container">
        <div class="card">
            <div class="card-header">
                Params
            </div>
            <div class="card-body">
                    <code>
                        <%= debug(params) %>
                    </code>
            </div>
        </div>
    </div>
<% end %>